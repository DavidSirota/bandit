{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core IPC only: receive context/task events and call the app's own load_pet/save_pet commands. No fs, shell, http, or dialog plugin is enabled, so the webview still cannot reach the filesystem, a shell, or the network.",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
